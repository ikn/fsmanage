"""
 * action wrapping a core action to give it an icon, mnemonic name, role(?), etc.
 * manager (sits on an ActionManager with navigation)
    * columns can show arbitrary metadata, with custom formatters
 * address bar (tied to a manager (core manager?  navigation history?))
    * takes sep and root to pass to render_path
"""
